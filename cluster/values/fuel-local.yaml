# custom overrides to `charts/fuel-local/values.yaml`

# chart settings
disableResourceLimits: false
waitForServiceDependencies: true
fuelSharedMnt:
  accessModes:
    value: "ReadWriteOnce"

# service settings
gqlApi:
  enabled: true
monitoring:
  enabled: true
elasticsearch:
  enabled: true
jaeger:
  enabled: true
kibana:
  enabled: true
surrealdb:
  enabled: true
fuelPublisher:
  enabled: true

# disabled services
