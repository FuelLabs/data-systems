extends:
  - "@commitlint/config-conventional"

rules:
  body-max-line-length:
    - 0
    - always
    - Infinity
  type-enum:
    - 2
    - always
    - [build, ci, docs, feat, fix, perf, refactor, test]
  subject-case:
    - 0
    - always
    - []
