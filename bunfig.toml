[install]
# Use exact versions for better reproducibility
exact = true

# Enable strict mode for better security
strict = true

# Configure registry
registry = "https://registry.npmjs.org"

[test]
# Configure test coverage
coverage = true

[install.scopes]
fuel-streams = "https://registry.npmjs.org"
