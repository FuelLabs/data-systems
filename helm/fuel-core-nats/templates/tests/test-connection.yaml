apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fuel-core-nats.fullname" . }}-test-connection"
  labels:
    {{- include "fuel-core-nats.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fuel-core-nats.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
