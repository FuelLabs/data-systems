apiVersion: v1
kind: Service
metadata:
  name: {{ include "fuel-core-nats.fullname" . }}
  labels:
    {{- include "fuel-core-nats.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fuel-core-nats.selectorLabels" . | nindent 4 }}
